extends layout
block head
block content
    h1= Locale('lbl.error')
    .content-container
        fieldset
            h2= error.status + " - " + message
            label= Locale('lbl.errorautoredirect')